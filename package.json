{
  "name": "jee-neet-platform",
  "private": true,
  "version": "0.1.0",
  "description": "Multi-tenant assessment platform for Indian JEE and NEET aspirants",
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r --filter './packages/**' build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm -r typecheck",
    "lint:sql": "node scripts/lint-sql.mjs",
    "lint:rls": "node scripts/check-rls.mjs",
    "scan:secrets": "node scripts/scan-secrets.mjs",
    "gates:self-test": "node scripts/lint-sql.mjs --self-test && node scripts/check-rls.mjs --self-test && node scripts/scan-secrets.mjs --self-test",
    "db:sync": "pnpm --filter @platform/db db:sync",
    "db:guard": "pnpm --filter @platform/db db:guard",
    "db:deploy": "pnpm --filter @platform/db db:deploy",
    "db:status": "pnpm --filter @platform/db db:status",
    "db:pull": "pnpm --filter @platform/db db:pull",
    "verify": "pnpm gates:self-test && pnpm typecheck && pnpm test && pnpm lint:sql && pnpm lint:rls && pnpm db:guard && pnpm scan:secrets"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@vitest/coverage-v8": "^2.1.8",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
