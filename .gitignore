node_modules/
dist/
build/
coverage/
.turbo/
*.tsbuildinfo

# env — never commit credentials
.env
.env.*
!.env.example

# prisma client — generated from the live database by `pnpm db:pull`, and
# regenerated on install. Committing it means shipping a snapshot that
# disagrees with the migrations the moment either side moves.
packages/db/generated/

# supabase local
supabase/.branches/
supabase/.temp/

# expo
.expo/
apps/mobile/ios/
apps/mobile/android/

.DS_Store
*.log
